# Simplified Dockerfile for pre-built binaries
# Used by 'make deploy' for fast deployments
FROM alpine:latest
RUN apk add --no-cache tzdata
ENV TZ=America/New_York
WORKDIR /app
COPY bin/tapedeck bin/tapedeck-cli ./
COPY cmd/tapedeck/web/ ./web/
EXPOSE 8080
CMD ["./tapedeck"]
